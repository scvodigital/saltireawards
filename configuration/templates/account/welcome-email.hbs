  [
  {{!-- Don't try to send if there is no email --}}
  {{#if @root.data.accountInfo.email}}
  {
  "from": "hello@saltireawards.org.uk",
  "h:sender": "hello@saltireawards.org.uk",
  "to": "{{{@root.data.accountInfo.email}}}",
  "o:tag": "welcome",
  "o:tracking": true,
  {{!-- "o:testmode": true, --}}
  "tag":  "welcome",
  "id":  "welcome",
  "connectionName": {{{stringify (getProperty @root.context.metaData.emailSendSettings 'welcome.mailgunConnection')}}},
  "subject":
  {{~#blockStringify this ~}}
    {{~>
    (getProperty @root.context.metaData.emailSendSettings 'welcome.subjectPartial')
      this
    ~}}
  {{~/blockStringify}},
  "html":
  {{~#blockStringify this ~}}
    {{~>
    (getProperty @root.context.metaData.emailSendSettings 'welcome.bodyPartial')
      this
    ~}}
  {{~/blockStringify}}
  }
  {{/if}}
  ]