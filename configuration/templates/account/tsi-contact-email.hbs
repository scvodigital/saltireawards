{{~#with (getProperty @root.context.metaData.tsiList (slugify @root.data.search_data.hits.hits.[0].district)) ~}}
{{~#each contacts ~}}
{{~#if (regexMatch @root.data.search_data.hits.hits.[0].place @key "gi") ~}}
{{email}}
{{~/if ~}}
{{~/each ~}}
{{~/with ~}}